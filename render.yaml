services:
  - type: web
    name: chat-app
    runtime: java21  # Change 'java' to 'java21'
    plan: free
    buildCommand: ./mvnw package -DskipTests
    startCommand: java -jar target/chat-app-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 21