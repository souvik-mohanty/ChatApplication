services:
  - type: web
    name: chat-app
    env: docker # OR omit and let Render detect Java from commands
    plan: free
    branch: main
    buildCommand: "./mvnw clean package -DskipTests"
    startCommand: "java -jar target/*.jar"
